<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anjoslogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Listagem.ListarFamilias"></activity>
        <activity android:name=".Recycler.ActivityRecycler" />
        <activity android:name=".SelecionarAcao" />
        <activity android:name=".Usuario.Perfil" />
        <activity android:name=".ResetarSenha" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Usuario.CadastrarUsuario" />
        <activity android:name=".Usuario.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>